# Linked list

Its a data structure that contain tail, head and length.


 Linked list containt list of Nodes. A node contains value and pointer(which points to next node or previous node). 

 



Q. Array v/s Linked List
  Array have indexes and list not. Linked list prefrable if we have to use push and pop elements operations insted of array because in array after insertion or deletion we have reindex other elment not in list.