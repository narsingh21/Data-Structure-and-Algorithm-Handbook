---------------------------------------Priority Queue----------------------

It's a data structure where each element has priority. Element with higher priority are served before with lower priority.

Priority queue is abstract data structure can be implement using List , array, heap etc...